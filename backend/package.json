{
  "name": "guess-it-backend",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker and Durable Object backend for Guess It",
  "type": "module",
  "scripts": {
    "predev": "npm run cf-typegen",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "typecheck": "npm run cf-typegen && tsc --noEmit",
    "cf-typegen": "wrangler types .worker-types.d.ts"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "typescript": "^6.0.2",
    "wrangler": "^4.135.0"
  }
}
