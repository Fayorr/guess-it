{
  "name": "guess-it-backend",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker and Durable Object backend for Guess It",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "typescript": "^6.0.2",
    "wrangler": "^4.135.0"
  }
}
